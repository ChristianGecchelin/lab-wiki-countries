import { Link } from 'react-router-dom';
const CountryDetails = (props) => {
  const { country } = props;
  console.log(country);
  return (
    <div>
      <Link to=""></Link>
    </div>
  );
};

export default CountryDetails;
